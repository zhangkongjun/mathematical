# AGENTS.md

## 目录职责
本目录存放清洗、转换、聚合后的可复用数据。

## 使用规则
- 这里只放派生数据，不要混入原始下载文件。
- 表格默认导出为 `CSV`，若使用其他格式，需要说明原因。
- 生成文件时保留来源信息，至少能追溯到输入文件名、处理脚本和关键参数。
- 覆盖已有结果前先确认是否需要保留版本副本或时间戳。
- 处理结果应服务于 `wl/`、`notebooks/` 或 `results/`，不要在这里堆放临时草稿。
