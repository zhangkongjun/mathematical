# AGENTS.md

## 目录职责
本目录存放概率统计主题的探索型 notebook。

## 使用规则
- 分布定义、采样函数和统计计算优先沉淀到 `wl/probability/`。
- notebook 负责说明实验设计、展示分布性质和验证结果。
- 随机实验必须显式记录随机种子与样本规模。
- 导出结果时区分临时展示产物与正式结果，分别写入 `notebooks/exports/` 或 `results/`。
