# AGENTS.md

## 目录职责
本目录存放 notebook 导出的图片、表格和其他展示文件。

## 使用规则
- 这里只保存导出产物，不要把源码脚本或长期手工编辑文档放到这里。
- 图形默认导出为 `PNG`，表格默认导出为 `CSV`。
- 文件名应包含来源 notebook 或主题信息，避免出现无上下文的默认名字。
- 若导出物属于正式结果，优先同步到 `results/` 的对应子目录。
