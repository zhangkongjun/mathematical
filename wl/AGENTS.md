# AGENTS.md

## 目录职责
本目录存放 Mathematica/Wolfram 语言核心代码。

## 使用规则
- 公共与可复用逻辑优先放在这里，不要仅存在 notebook 单元格中。
- 新代码优先写成 `.wl` 或 `.wls`，并按领域子目录归类。
- 保持函数接口清晰，显式传入输入路径、参数和输出位置，避免硬编码环境依赖。
- 变更后优先通过最小 `wolframscript` 或脚本入口做验证。
