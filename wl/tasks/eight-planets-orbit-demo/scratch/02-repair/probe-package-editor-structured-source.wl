(* ::Package:: *)

(* ::Title:: *)
(* Package Editor Structured Source Probe *)

(* ::Text:: *)
(* 这是一段用于验证 package editor 结构化单元标记的说明文字。 *)

(* ::Section:: *)
(* 第一节 *)

(* ::Text:: *)
(* 如果 Front End 能把这里识别成 Text 单元，后续就可以把说明文档也先放进 .wl 源文件。 *)

probeValue = 1 + 1;

(* ::Section:: *)
(* 第二节 *)

(* ::Text:: *)
(* 这里再放一段中文说明，用于确认中文文本不会在 package editor 打开阶段被破坏。 *)

probeValue
