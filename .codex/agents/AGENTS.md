# AGENTS.md

## 目录职责
本目录存放面向 Codex 的代理定义文件。

## 使用规则
- 每个代理保持单一职责，文件名应能直接体现用途。
- 代理定义优先写成声明式配置，不要把大量业务逻辑硬编码进代理文案。
- 新增或修改代理时，先核对其是否与 `wl/`、`scripts/`、`notebooks/` 的实际流程一致。
- 代理说明里涉及输入、输出、日志路径时，使用仓库内约定目录。
- 不在代理配置中写入真实密钥、账户或外部环境专属路径。
